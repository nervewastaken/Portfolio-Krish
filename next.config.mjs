/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "raw.githubusercontent.com",
      "upload.wikimedia.org",
      "miro.medium.com",
    ],
  },
};

export default nextConfig;
